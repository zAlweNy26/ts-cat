[Overview](../index.md) / isHook

# isHook()

> **isHook**(`hook`): `hook is Hook<keyof HookTypes>`

## Parameters

| Parameter | Type |
| ------ | ------ |
| `hook` | `any` |

## Returns

`hook is Hook<keyof HookTypes>`
